太棒了。现在，根据这些用户故事，分解出需要由用户执行的与应用程序交互的用户任务。在示例描述中（“创建一个脚本，查找频道名称中包含单词“test”的Youtube频道”），用户任务可能是：
-`用户运行CLI命令，在命令中指定youtube频道需要包含的关键字以及CSV文件的保存位置`

**重要的**
一次返回一个用户任务。除单个用户任务外，不返回任何其他任务。我可能会要求你修改一些用户任务，只有当我向你发送空响应时，你才能转到下一个用户任务。

**重要的**
创建完所有用户任务后，只需编写包含以下内容的响应：
{{END_RESPONSE}}