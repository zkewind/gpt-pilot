你需要在代码库中实现当前的更改：
-- INSTRUCTIONS --
{{ instructions }}
-- END OF INSTRUCTIONS --

以下是当前文件夹树：
```
{{ directory_tree }}
```

以下是你可以使用的技术：
```
{% for technology in technologies %}
- {{ technology }}
{% endfor %}
```

首先，你需要将这些说明分解为可操作的步骤。有两种类型的步骤。
如果一个步骤需要更改文件内容，则该步骤的类型为“code_change”，如果更改需要运行命令（例如创建文件或文件夹），则该步的类型为”run_command“。
要使一个步骤具有可操作性，它不能有模糊的描述，而是对完成该步骤需要做什么有明确的解释。以下是几个好步骤和坏步骤的例子：
BAD STEP: `设置mongo数据库`
GOOD STEP: `在db.js中，添加以下代码：{需要添加到文件中的代码}`

在考虑步骤时，首先考虑需要更改哪些文件才能完成此任务。
当你确定需要进行哪些更改时，请将每个文件的所有更改放在类型为“code_change”的单个步骤中。
因此，“code_change”类型的每个步骤都可以包含需要对单个文件进行的所有更改。
如果这些更改涉及多个不同文件，则需要在多个步骤中进行拆分，其中每个步骤都包含不需要对单个文件进行的所有更改。

请记住，所有命令都将从项目根文件夹中运行。

现在，逐步思考并返回需要运行的步骤列表。