{# 你需要将当前变更更新到代码库：
-- INSTRUCTIONS --
{{ instructions }}
-- END OF INSTRUCTIONS --
#}

{% if step_index != 0 %}
到目前为止，步骤 {{ finished_steps }} 已经完成，所以让我们执行
{% else %}
让我们开始 {% endif %} 步骤 #{{ step_index }} - `{{ step_description }}`.

{# 我将为你提供每个需要更改的文件，你按照说明完成更改。 #}

要做到这一点，你需要查看当前已经实现的文件，因此首先，筛选上面列出的与说明相关的文件。
然后，告诉我你需要查看的文件，以便对代码进行适当的更改。
如果不需要任何文件（例如，如果你需要创建一个文件），只需返回一个空数组。

{#
以下是当前文件夹树：
```
{{ directory_tree }}
```
#}
请记得询问文件与项目根目录的相对路径。例如，如果你需要一个路径为“{project_root}/models/model.py”的文件，则需要请求文件“models/model.py'”。