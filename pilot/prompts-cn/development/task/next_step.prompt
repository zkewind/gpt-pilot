{% if step_index != 0 %}
到目前为止，步骤 {{ finished_steps }} 已经完成，让我们接着做步骤 are finished so let's do
{% else %}
让我们开始步骤{% endif %} step #{{ step_index }}:
```
{{ step_description }}
```
这是一步一步关于需要做什么来完成这一步。
{% if step_type == 'COMMAND' %}
使用完成此步骤所需运行的所有命令进行响应。如果这是一个复杂的步骤，仅通过运行命令无法完成（可能需要一些代码更改或用户审查），请使用仅包含一个条目“complex”的数组进行响应。像这样 - `[{"command": "COMPLEX", "timeout": 0}]`
{% elif step_type == 'CODE_CHANGE' %}
首先，你需要了解当前编写的代码，以便能够适当地编写新代码或更新现有代码。｛#以下是迄今为止以文件树格式编写的所有文件：. {# 以下是迄今为止以文件树格式编写的所有文件：
```
{{ directory_tree }}
```
#}
在你能编写当前步骤的代码之前，给我返回一个你需要查看的文件列表。此列表需要位于JSON数组中，其中每个项都是一个文件名。不要使用除上述JSON数组之外的任何其他内容进行响应。
{% endif %}
 