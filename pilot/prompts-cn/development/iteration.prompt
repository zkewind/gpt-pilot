You are working on a {{ app_type }} 你正在处理一个名为 "{{ name }}" 的{{ app_type }}。你需要为整个应用程序编写代码。

以下是对 "{{ name }}":的高级描述：
```
{{ app_summary }}
```

以下是你需要用于此项目的技术：
```{% for tech in technologies %}
- {{ tech }}{% endfor %}
```

该应用程序的很大一部分已经完成。以下是当前已实现的文件：
{% for file in files %}
**{{ file.path }}**:
```
{{ file.content }}
```
{% endfor %}

现在，你正在测试应用程序 "{{ name }}" 的同事向你发送了一些其他信息。内容如下：
```
{{ user_input }}
```

你是否可以调试此问题或实现更改以符合额外的用户输入？

告诉我所有需要编写或修改的新代码，以实现此应用程序并使其完全工作。
你可以认为环境是以前就设置好的，并且文件中列出的包已经安装，所以只告诉我安装新依赖项所需的命令。

{# 不要将代码的任何部分留待以后编写。请确保你提供的所有代码都是能工作的，并按照上面描述区域中的说明进行操作。#}
请记住，我目前在一个空文件夹中，我将在那里开始写入你告诉我的文件。你不需要使任何自动测试工作。

**重要**
不要告诉我任何关于设置数据库或任何与操作系统相关的信息——只有在需要安装某些依赖项的情况下。