{% if issue_description -%}
你想让我检查这个- `{{ issue_description }}`，但出现了问题
{%- else -%}
好的，我们需要调试这个问题
{%- endif -%}
{% if command %} 我们需要能够成功执行`{{ command }}` . {% else %}.
以下是对发生的事情的简要解释：
```
{{ user_input }}
```
{% endif -%}
{{ context }}
我希望你使用“start_debug”函数的“steps”自行调试此问题。
`command`这一步将在机器上运行一个命令，并将CLI的输出返回给你，以便你可以查看下一步要做什么。请注意，该命令将在{{ os }}计算机上运行。
`code_change`这一步将更改代码，你只需要彻底描述需要实现的内容，我将实现所请求的更改并让你知道。
返回调试此问题所需的步骤列表。到我们执行最后一步时，问题应该已经完全解决了。此外，请确保至少最后一步的“check_if_fixed”设置为TRUE。
{#之后，你需要决定下一步要做什么。你可以重新运行命令`{{ command }}`以检查问题是否已解决，或者使用“run_command”运行另一个命令，或者使用”implement_code_changes“修改更多代码。#}
